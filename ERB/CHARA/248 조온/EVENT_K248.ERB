;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;eratohoYM Mk-2 rev.6용 분할형 대사 템플릿
;X1을 캐릭터 번호 치환해 사용해주세요(레이무라면 1, 렌코라면 49, 미마라면 100).
;
;이 파일에는 조교 개시시 · 종료시 · 조교 중 이벤트 · 엔딩 대사용 함수 등이 마련되어 있습니다.
;≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡≡
;==================================================
;○○ 대사
;해설 따위를 여기에 적으면 됨
;CFLAG:5201 = 범용(분할형) 대사에서 조교 첫회 판정으로서 이용
;CFLAG:5002~5009 = 범용(단독형) 대사에서 조수 입장 첫회 판정으로서 이용
;CFLAG:5300~5309 = 범용(분할형) 대사에서 절정 첫회 판정으로서 이용
;CFLAG:5310~5319 = 범용(분할형) 대사에서 각인 첫회 판정으로서 이용
;CFLAG:5320~5355 = 범용(분할형) 대사에서 파라미터 첫회 판정으로서 이용
;CFLAG:7000~7999 = 범용(분할형) 대사에서 커맨드 여러회 판정으로서 이용 @COM_FLAG2 참조
;CFLAG:8900~8905 = 범용(분할형) 대사에서 자위 여러회 판정으로서 이용 @COM_FLAG2 참조
;CFLAG:9000~9099 = 범용(분할형) 대사에서 커맨드 첫회 판정으로서 이용 @COM_FLAG 참조
;==================================================
;==================================================
;대사 파일의 존재 판정
;==================================================
;대사의 존재 판정(YM판)
@TRY_248
RETURN 1

;==================================================
;엑스트라 모드 구문
;본인의 이상 속 색시를 표현하는데 쓴다
;==================================================
@SET_CHARA_KOJO_EXTRA_248
;PRINTFORML 「엑스트라 모드의 선택입니다」
;PRINTL [0] 예
;PRINTL [1] 아니오
;$INPUT_LOOP_1
;INPUT
;IF RESULT == 0
    ;PRINTFORMW 엑스트라 모드 실행
;ELSEIF RESULT == 1
    ;PRINTFORMW 엑스트라 모드 무시
;ELSE
    ;GOTO INPUT_LOOP_1
;ENDIF

;==================================================
;조교 개시 대사 부분
;==================================================
@EVENTTRAIN
;대사 표시를 OFF했을 경우에는 돌아온다
SIF FLAG:11 <= 0
    RETURN 0
SIF NO:TARGET != 248
    RETURN 0
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;재갈이 있을 경우에도 돌아온다
SIF TEQUIP:42
    RETURN 0
;기절 중에도 돌아온다
SIF TFLAG:899 >= 1
    RETURN 0

DRAWLINE

;조교 개시 대사 분기
IF CFLAG:5201 == 0
    TRYCALL K248_MESSAGE_START_1
ELSE
    TRYCALL K248_MESSAGE_START_2
ENDIF

;-------------------------------------------------
;첫조교시
;-------------------------------------------------
@K248_MESSAGE_START_1

;시온이 조수일때
IF ASSINO() == 249
    IF TALENT:120
        PRINTFORMW 「여긴 어디야?, 넌 대체 누구지?」
        PRINTFORMW 아직 아무것도 모르는 듯한 온몸에 화려한 옷가지와 보석들로 치장한 소년이 내게 물어왔다.
        PRINTFORMW 이에, 나는 내가 바로 너의 새로운 주인이며, 나의 명령에 따라야 한다고 말해주었다.
        PRINTFORMW 거만하고 고상한 척 소년은 내 말에 콧방귀를 꼈다.
        PRINTFORMW 「이렇게 만들어 놓고서 주인이라니! 빨리 원래대로 만들어 놓고 풀어줘!」
        PRINTFORMW 소년은 내게 항의했지만 받아질리는 없었다.
        PRINTFORMW 원래대로 돌아오고 싶다면, 또 여기서 나가고 싶다면 내 말에 복종하는 것이 좋다고 말했다.
        PRINTFORMW 「쳇, 그런다고 내가 말에 따를것 같아?」
        PRINTFORMW 여전히 건방진 소년의 옷을 거칠게 벗겨내었다.
        PRINTFORMW 「이런 짓을 하고도 무사할 것 같아!」
        PRINTFORMW 하지만 곧이어 들어오는 익숙한 모습에 소년의 모습은 굳어가기 시작했다.
        PRINTFORMW 「어…… 언니? 어째서 여기에……,」
        PRINTFORMW 말없는 시온을 대신해 나는 시온의 옷을 벗겨내었다.
        PRINTFORMW 건방진 철부지 소녀의 표정이 점점 공포로 물들어가고 있었다.
    ELSE
        PRINTFORMW 「여긴 어디야?, 넌 대체 누구지?」
        PRINTFORMW 아직 아무것도 모르는 듯한 온몸에 화려한 옷가지와 보석들로 치장한 소녀가 내게 물어왔다.
        PRINTFORMW 이에, 나는 내가 바로 너의 새로운 주인이며, 나의 명령에 따라야 한다고 말해주었다.
        PRINTFORMW 거만하고 고상해보이는 소녀는 선글라스를 치켜세우더니 내 말에 콧방귀를 꼈다.
        PRINTFORMW 「장난은 재미없으니까 그만 보내주지 않을래? 귀찮으니까 돈으로……,」
        PRINTFORMW 소녀는 주머니를 털어보았지만 그녀의 주머니 속에는 아무것도 없었다.
        PRINTFORMW 찾는것은 이것인가, 하고 나는 그녀의 돈뭉치를 꺼냈다.
        PRINTFORMW 다가오는 소녀의 팔을 붙잡곤 돌려받고 싶다면 내 명령에 따라야한다고 말했다.
        PRINTFORMW 「쳇, 그런다고 내가 말에 따를것 같아?」
        PRINTFORMW 여전히 건방진 소녀의 옷을 거칠게 벗겨내었다.
        PRINTFORMW 「이런 짓을 하고도 무사할 것 같아!」
        PRINTFORMW 하지만 곧이어 들어오는 익숙한 모습에 소녀의 모습은 굳어가기 시작했다.
        PRINTFORMW 「어…… 언니? 어째서 여기에……,」
        PRINTFORMW 말없는 시온을 대신해 나는 시온의 옷을 벗겨내었다.
        PRINTFORMW 건방진 철부지 소녀의 표정이 점점 공포로 물들어가고 있었다.
    ENDIF
ELSE
    IF TALENT:120
        PRINTFORMW 「여긴 어디야?, 넌 대체 누구지?」
        PRINTFORMW 아직 아무것도 모르는 듯한 온몸에 화려한 옷가지와 보석들로 치장한 소년이 내게 물어왔다.
        PRINTFORMW 이에, 나는 내가 바로 너의 새로운 주인이며, 나의 명령에 따라야 한다고 말해주었다.
        PRINTFORMW 거만하고 고상한 척 소년은 내 말에 콧방귀를 꼈다.
        PRINTFORMW 「이렇게 만들어 놓고서 주인이라니! 빨리 원래대로 만들어 놓고 풀어줘!」
        PRINTFORMW 소년은 내게 항의했지만 받아질리는 없었다.
        PRINTFORMW 원래대로 돌아오고 싶다면, 또 여기서 나가고 싶다면 내 말에 복종하는 것이 좋다고 말했다.
        PRINTFORMW 「쳇, 그런다고 내가 말에 따를것 같아?」
        PRINTFORMW 여전히 건방진 소년의 옷을 거칠게 벗겨내었다.
    ELSE
        PRINTFORMW 「여긴 어디야?, 넌 대체 누구지?」
        PRINTFORMW 아직 아무것도 모르는 듯한 온몸에 화려한 옷가지와 보석들로 치장한 소녀가 내게 물어왔다.
        PRINTFORMW 이에, 나는 내가 바로 너의 새로운 주인이며, 나의 명령에 따라야 한다고 말해주었다.
        PRINTFORMW 거만하고 고상해보이는 소녀는 선글라스를 치켜세우더니 내 말에 콧방귀를 꼈다.
        PRINTFORMW 「장난은 재미없으니까 그만 보내주지 않을래? 귀찮으니까 돈으로……,」
        PRINTFORMW 소녀는 주머니를 털어보았지만 그녀의 주머니 속에는 아무것도 없었다.
        PRINTFORMW 찾는것은 이것인가, 하고 나는 그녀의 돈뭉치를 꺼냈다.
        PRINTFORMW 다가오는 소녀의 팔을 붙잡곤 돌려받고 싶다면 내 명령에 따라야한다고 말했다.
        PRINTFORMW 「쳇, 그런다고 내가 말에 따를것 같아?」
        PRINTFORMW 여전히 건방진 소녀의 옷을 거칠게 벗겨내었다.
    ENDIF
ENDIF
CFLAG:5201 = 1

;-------------------------------------------------
;조교 개시시
;-------------------------------------------------
@K248_MESSAGE_START_2
;시온이 조수
IF ASSI > 0 && ASSINO() == 249
    ;반발 3
    IF MARK:9 == 3
        PRINTFORMW 「언니라도, 이런건 못참아……!」
        PRINTFORMW 「언니는, 항상 나에게 빌붙어 살았잖아!」
        PRINTFORMW 『미안해, 조온……』
        PRINTFORMW 『하지만, 이젠 너에게 빌붙을 일은 없을꺼야……』
    ;쾌락 3, 공포,반발 0
    ELSEIF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
        PRINTFORMW 「언니……, 오늘도……?」
        PRINTFORMW 『……기다리고 있었니?』
    ;기타
    ELSE
        PRINTFORMW 「언니……, 오늘도……?」
        PRINTFORMW 『…………,』
    ENDIF
;공포 3, 반발 3미만
ELSEIF MARK:8 == 3 && MARK:9 < 3
    ;굴복 3
    IF MARK:2 == 3
        PRINTFORMW 「이제, 훌쩍, 시키는대로 잘, 흣, 할테니까……」
        PRINTFORMW 공포에 사로잡힌 소녀는 훌쩍거리며 스스로 옷가지를 벗기 시작했다.
    ELSE
        PRINTFORMW 「어차피 또 홀딱 벗겨서 이상한 짓 하려는 거 겠지……?」
        PRINTFORMW 조온은 체념한 듯 스스로 옷을 벗기 시작했다.
    ENDIF
;반발 3
ELSEIF MARK:9 == 3
    ;공포 3
    IF MARK:8 == 3
        PRINTFORMW 「계속 이렇게 괴롭히면 가만 안둘꺼야!」
        PRINTFORMW 조온은 분명 떨리는 목소리로 말했다.
    ;반발만 3
    ELSE
        PRINTFORMW 「돈이 다 떨어지기 전에 날 풀어주는게 좋을껄?」
        PRINTFORMW 「내 능력이라면 빈털털이가 되는건 시간문제일텐데!」
        PRINTFORMW 자신의 능력이 차단되었다는 걸 아는지 모르는지 조온은 의기양양하게 말하고 있었다.
    ENDIF
;쾌락 3, 공포,반발 0
ELSEIF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
    PRINTFORMW 「어차피 이상한 짓 할꺼잖아……,」
    PRINTFORMW 조온은 스스로 옷가지를 벗어 정리해 두고 있었다.
;상애친애연모
ELSEIF TALENT:150 || TALENT:152 || TALENT:153
    PRINTFORMW 「시작해줘……,」
    PRINTFORMW 조온은 스스로 옷가지를 벗어 정리해 두고 있었다.
;기타
ELSE
    PRINTFORMW 「이제 그만 보내주지 않겠어?」
    PRINTFORMW 「이정도면 충분하잖아!」
    PRINTFORMW 애원하는 조온의 옷을 거칠게 벗겨냈다.
ENDIF
;==================================================
;조교 종료시의 대사
;==================================================
@EVENTEND
;대사 표시를 OFF했을 경우에는 돌아온다
SIF FLAG:11 <= 0
    RETURN 0
SIF NO:TARGET != 248
    RETURN 0
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;재갈이 있을 경우에도 돌아온다
SIF TEQUIP:42
    RETURN 0
;기절 중에도 돌아온다
SIF TFLAG:899 >= 1
    RETURN 0
DRAWLINE
TRYCALL K248_MESSAGE_END

;--------------------------------------------------
;조교 종료시의 대사
;--------------------------------------------------
@K248_MESSAGE_END

;쾌락 2이상, 연모 친애 상애
IF MARK:1 >= 2 && (TALENT:150 || TALENT:152 || TALENT:153)
    IF PALAM:13 >= PALAMLV:3
        PRINTFORML 「후아……, 하아……,」
        PRINTFORMW 「또……, 또 하자……, 약속해 줘……,」
        PRINTFORMW 숨이 찬 듯 숨을 고르는 조온은 아쉽다는 표정으로 방을 나서는 나를 바라보았다.
    ELSE
        PRINTFORMW 「벌써 끝났어……?」
        PRINTFORMW 조온은 조금 아쉬운 듯 하다.
    ENDIF
;시온이 조수
ELSEIF ASSINO() == 249
    IF PALAM:13 >= PALAMLV:3
        PRINTFORML 「하아……, 하아……,」
        PRINTFORMW 「……그만 나가 줘……,」
        PRINTFORMW 조온 지친 듯 시온을 방에서 쫒아냈다.
    ELSE
        PRINTFORMW 「언니마저……,」
        PRINTFORMW 눈물을 보이며 뒤도는 조온을 뒤로하고 시온은 나와 방을 나왔다.
    ENDIF
;공포3
ELSEIF MARK:8 == 3
    PRINTFORMW 「……내일도 할꺼야……?,」
    PRINTFORMW 두려움에 떠는 조온을 뒤로하고 방을 나왔다.
;통상
ELSE
    IF PALAM:13 >= PALAMLV:3
        PRINTFORMW 「……조금 지치게 했다고 굴복할줄 알아……?」
        PRINTFORMW 여자의 즐거움을 알게 됬지만 인정하기 싫은 듯 조온은 방에서 쫒아냈다.
    ELSE
        PRINTFORMW 「이제 끝났어? 그럼 나가……,」
        PRINTFORMW 조온은 지친 듯 이쪽을 보지도 않고 돌아 누웠다.
    ENDIF
ENDIF

;==================================================
;@KOJO_MESSAGE_PALAMCNG 관계
;패러미터 변동이 방아쇠인 대사
;패러미터 변동 후에 대사를 발동합니다
;==================================================
@KOJO_MESSAGE_PALAMCNG_248
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;재갈이 있을 경우에도 돌아온다
SIF TEQUIP:42
    RETURN 0
;기절 중에도 돌아온다
SIF TFLAG:899 >= 1
    RETURN 0
;처음으로 PALAM이 LV2 넘었을 때의 대사 호출
FOR LOCAL, 0, 35
    SIF PALAM:LOCAL + UP:LOCAL > PALAMLV:2 && CFLAG:(5320 + LOCAL) == 0
        TRYCALLFORM K248_MESSAGE_OVERLV2_{LOCAL}
NEXT
;절정시의 대사 호출
FOR LOCAL, 0, 50
    SIF NOWEX:LOCAL
        TRYCALLFORM K248_MESSAGE_ORGASM_{LOCAL}
NEXT
;사정시켰다
FOR LOCAL, 8, 30
    SIF TFLAG:LOCAL
        TRYCALLFORM K248_MESSAGE_EJAC_PLAYER_{LOCAL}
NEXT

;==================================================
;처음으로 PALAM이 LV2 넘었을 때의 대사 CFLAG 5320～5355을 사용
;==================================================
;-------------------------------------------------
;처음으로 윤활이 LV2 넘었다 CFLAG:5330
;-------------------------------------------------
@K248_MESSAGE_OVERLV2_10
;시온이 조수
IF ASSI > 0 && ASSINO() == 249
    PRINTFORMW 「으앗, 잠깐만……!, 안에서 흘러나와……!」
    PRINTFORMW 『잔뜩 젖었구나……,』
ELSE
    PRINTFORMW 「잠깐만……, 그만 만져……,」
    PRINTFORMW 조온의 몸에서 흘러나온 애액이 하반신을 온통 더렵혔다.
ENDIF
CFLAG:5330 = 1

;-------------------------------------------------
;처음으로 욕정이 LV2 넘었다 CFLAG:5333
;-------------------------------------------------
@K248_MESSAGE_OVERLV2_13
;시온이 조수
IF ASSI > 0 && ASSINO() == 249
    PRINTFORMW 「기분……, 기분이 이상해……」
    PRINTFORMW 「보……, 보지마 언니……,」
ELSE
    PRINTFORMW 「기분……, 이상해……,」
    PRINTFORMW 조온은 왠지 이쪽을 원하는 듯한 표정을 하고 있다.
ENDIF
CFLAG:5333 = 1

;-------------------------------------------------
;처음으로 치정이 LV2 넘었다 CFLAG:5335
;-------------------------------------------------
@K248_MESSAGE_OVERLV2_15
;시온이 조수
IF ASSI > 0 && ASSINO() == 249
    PRINTFORMW 「언니……, 기분이, 이상해……,」
    PRINTFORMW 『조온……,』
ELSE
    PRINTFORMW 「무슨 짓을 한거야? 기분이 이상하잖아……,」
    PRINTFORMW 조온의 얼굴이 붉게 상기되었다.
ENDIF
CFLAG:5335 = 1

;-------------------------------------------------
;처음으로 공포가 LV2 넘었다 CFLAG:5337
;-------------------------------------------------
@K248_MESSAGE_OVERLV2_17
;시온이 조수
IF ASSI > 0 && ASSINO() == 249
    PRINTFORMW 「그만 손 떼……,」
    PRINTFORMW 시온이 다가가지만 조온은 고개를 돌려 움츠렸다.
ELSE
    PRINTFORMW 「그만……, 그만해 줘……,」
    PRINTFORMW 조온은 눈물을 흘리며 고개를 돌렸다.
ENDIF
CFLAG:5337 = 1

;==================================================
;절정시의 대사 CFLAG 5300～을 사용
;==================================================
;-------------------------------------------------
;Ｃ절정 CFLAG:5300
;-------------------------------------------------
@K248_MESSAGE_ORGASM_0
;처음
IF CFLAG:5300 == 0
    ;시온이 조수
    IF ASSI > 0 && ASSINO() == 249
        IF TALENT:120 || TALENT:121
            PRINTFORMW 「자매끼리 이런거 이상해……!!!」
            PRINTFORMW 『조온……! 조온……!』
        ENDIF
    ;남자 혹은 후타(남성의 절정은 C절정판정)
    ELSEIF TALENT:120 || TALENT:121
        PRINTFORMW 「아앗! 나와버렷!」
        SIF ASSIPLAY && ASSINO() == 249
            PRINTFORMW 「언니, 언니의 안에 싸버렷……!」
        PRINTFORMW 『아……, 아핫……, 하흣!!』
    ;기타
    ELSE
        PRINTFORMW 「이거 뭐야!, 이상해!, 그만해!」
    ENDIF
    CFLAG:5300 = 1
;2회째 이후
ELSE
    ;쾌락 3, 공포,반발 0
    IF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
        PRINTFORMW 「나……, 나와! 클리토리스……, 꺄앗!!」
    ;Ｃ절정 50 이상
    ELSEIF EX:0 >= 50
        IF TALENT:120
            PRINTFORMW 「흐앗! 갈거같아! 사정할거 같아!!」
        ELSE
            PRINTFORMW 「움찔거려!, 그만!, 아-앗!!」
        ENDIF
    ELSE
        PRINTFORMW 「또 와버려!, 그만!, 그마안!!」
    ENDIF
ENDIF

;-------------------------------------------------
;Ｖ절정 CFLAG:5301
;-------------------------------------------------
@K248_MESSAGE_ORGASM_1
;처음
IF CFLAG:5301 == 0
    PRINTFORMW 「아앗!, 하앙, 머리가!!」
    CFLAG:5301 = 1
;2회째 이후
ELSE
    ;쾌락 3, 공포,반발 0
    IF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
        PRINTFORMW 「안에서……, 가버려……! 안쪽에서 울려와……! 하아앙!!」
    ;Ｖ절정 50 이상
    ELSEIF EX:1 >= 50
        PRINTFORMW 「오고있어, 안으로, 후아아앗!!」
    ELSE
        PRINTFORMW 「하아, 하앗! 또 와!!」
    ENDIF
ENDIF

;-------------------------------------------------
;Ａ절정 CFLAG:5302
;-------------------------------------------------
@K248_MESSAGE_ORGASM_2
;처음
IF CFLAG:5302 == 0
    PRINTFORMW 「흐아아! 엉덩이가! 이상해엣!!」
    CFLAG:5302 = 1
;2회째 이후
ELSE
    ;쾌락 3, 공포,반발 0
    IF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
        PRINTFORMW 「구멍……, 가버려……! 뱃속에서……! 울려!!」
    ;Ａ절정 50 이상
    ELSEIF EX:2 >= 50
        PRINTFORMW 「엉덩이로 가버려엇!, 흐아앙!!」
    ELSE
        PRINTFORMW 「하앗!, 하아앗!, 엉덩이가!!」
    ENDIF
ENDIF

;-------------------------------------------------
;Ｂ절정 CFLAG:5303
;-------------------------------------------------
@K248_MESSAGE_ORGASM_3
;처음
IF CFLAG:5303 == 0
    PRINTFORMW 「후앗! 이, 이상해엣!」
    CFLAG:5303 = 1
;2회째 이후
ELSE
    ;쾌락 3, 공포,반발 0
    IF MARK:1 == 3 && MARK:9 == 0 && MARK:8 == 0
        PRINTFORMW 「꼭지……, 가슴이……! 하앙……! 오고있어!!」
    ;Ｂ절정 50 이상
    ELSEIF EX:3 >= 50
        PRINTFORMW 「가슴, 무, 문지르지마아!!」
    ELSE
        PRINTFORMW 「후앗!, 가슴, 이,이상해!!」
    ENDIF
ENDIF

;-------------------------------------------------
;분유 CFLAG:5304
;-------------------------------------------------
@K248_MESSAGE_ORGASM_10
;처음
IF CFLAG:5304 == 0
    ;임신중
    IF TALENT:140
        PRINTFORMW 「나와앗!, 아기우유가앗!!」
    ;약을 썼겠지
    ELSE
        PRINTFORMW 「에엣? 나 아기우유가 나와?!」
    ENDIF
    CFLAG:5304 = 1
;2회째 이후
ELSE
    PRINTFORMW 「흐아, 다 뿜어버리면, 마르지 않을까……」
ENDIF

;-------------------------------------------------
;사정 CFLAG:5305
;-------------------------------------------------
@K248_MESSAGE_ORGASM_11
;처음
IF CFLAG:5305 == 0
    ;남자면
    IF TALENT:120
        PRINTFORMW 「하핫!, 나와!」
    ;후타라면
    ELSE
        PRINTFORMW 「모…… 못참겠어……!」
    ENDIF
    CFLAG:5305 = 1
;2회째 이후
ELSE
    PRINTFORMW 「하얀거…… 나와, 으앗!」
ENDIF

;-------------------------------------------------
;방뇨 CFLAG:5306
;방뇨 또는 강제방뇨 커맨드시에는 호출되지 않는다
;-------------------------------------------------
@K248_MESSAGE_ORGASM_12
SIF SELECTCOM == 111 || SELECTCOM == 620
    RETURN 0
;처음
IF CFLAG:5306 == 0
    PRINTFORMW 「하읏……,」
    CFLAG:5306 = 1
;2회째 이후
ELSE
    PRINTFORMW 「보, 보지마……,」
ENDIF

;-------------------------------------------------
;질내 사정 시늉(엉덩이, 균열)으로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_8
;PRINTFORMW 「질내 사정 시늉으로 사정시켰을 시의 대사입니다」

;-------------------------------------------------
;애널 섹스로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_9
;주인의 페니스
IF TFLAG:35
    ;남자라면
    IF TALENT:120
        PRINTFORMW 「아앗!, 엉덩이에, 뭔가 들어와……!!」
    ;아니면
    ELSE
        PRINTFORMW 「거기 아냐! 거기다 싸면 안되엣……!!」
    ENDIF
;그 외
ELSE
    ;남자라면
    IF TALENT:120
        PRINTFORMW 「나도 엉덩이로 느끼고있어……?」
    ;아니면
    ELSE
        PRINTFORMW 「아냐! 거기다 싸면………!!」
    ENDIF
ENDIF

;-------------------------------------------------
;섹스로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_10
;주인의 페니스
IF TFLAG:35
    IF TALENT:150 || TALENT:152 || TALENT:153
        PRINTFORMW 「와줘엇! 와줘!!」
    ELSE
        PRINTFORMW 「후아, 하읏, 으하, 따뜻한 거, 오고있어!!」
    ENDIF
;시온이 조수플
ELSEIF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「어,언니의 정액이……!!」
    PRINTFORMW 『조온…………!!』
;그 외
ELSE
    PRINTFORMW 「하흣, 으하, 아앙!!」
ENDIF

;-------------------------------------------------
;입으로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_11
;시온이 조수플
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 『조, 조온……!』
    PRINTFORMW 「……!, 키스한것 뿐 인데……?」
ELSE
    IF EXP:22 >= 49 && (TALENT:150 || TALENT:152 || TALENT:153)
        PRINTFORMW 「꺄앗, 내 입으로 간거야?」
    ELSE
        PRINTFORMW 「츄하……!, 갑자기 싸면 어떻해……!」
    ENDIF
ENDIF

;-------------------------------------------------
;손으로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_12
;시온이 조수플
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 『조, 조온……!』
    PRINTFORMW 「손에 잔뜩……,」
ELSE
    IF TALENT:150 || TALENT:152 || TALENT:153
        PRINTFORMW 「내손으로 갔구나……, 기뻐……,」
    ELSE
        PRINTFORMW 「으앗……!, 더러워!」
    ENDIF
ENDIF

;-------------------------------------------------
;커닐링구스 강제로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_13
PRINTFORMW 「흐흣, 후아!」

;-------------------------------------------------
;키스로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_14
IF TALENT:150 || TALENT:152 || TALENT:153 
    PRINTFORMW 「츕츕……, 그렇게 좋았어……?」
ELSE
    PRINTFORMW 「읍, 츄하……!」
    PRINTFORMW 「어디다 뿌리는거야!」
    ;시온이 조수플
    SIF ASSIPLAY && ASSINO() == 249
        PRINTFORMW 『미, 미안……,』
ENDIF
;-------------------------------------------------
;스마타로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_15
IF ABL:1 >= 2 && MARK:1 == 3
    PRINTFORMW 「너……, 넣어줘……」
ELSE
    PRINTFORMW 「꺄앗……!,」
ENDIF

;-------------------------------------------------
;머리카락으로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_16
PRINTFORMW 「머리 감기 힘들잖아……!」

;-------------------------------------------------
;조수를 범한다로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_17
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「어, 언니 안으로 가버려……!」
    PRINTFORMW 『에엣? 잠깐……!』
ELSE
    PRINTFORMW 「하아,아핫, 나와……!」
ENDIF
;-------------------------------------------------
;역강간으로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_18
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「어, 언니……! 그만해……!」
    PRINTFORMW 「나……, 나와버려……!」
ELSE
    PRINTFORMW 「하아아앗! 이상해엣!!」
ENDIF

;-------------------------------------------------
;발로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_19
PRINTFORMW 「후앗……!」

;-------------------------------------------------
;겨드랑이로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_20
PRINTFORMW 「……기분나빠……!」

;-------------------------------------------------
;펠라로 사정 후, 불결을 핥아낼 때
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_21
PRINTFORMW 「츄흡, 하앗, 츄하……,」

;-------------------------------------------------
;좆대로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_22
PRINTFORMW 「흐흣!, 나와……!」

;-------------------------------------------------
;요도로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_23
;PRINTFORMW 「요도로 사정시켰을 시의 대사입니다」

;-------------------------------------------------
;육변기로 사정
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_24
;PRINTFORMW 「육변기로 사정시켰을 시의 대사입니다」

;-------------------------------------------------
;가슴으로 조교자가 사정(핥지 못함)
;-------------------------------------------------
@K248_MESSAGE_EJAC_PLAYER_29
;PRINTFORMW 「가슴으로 조교자가 사정했을 시의 대사입니다」

;==================================================
;@KOJO_MESSAGE_MARKCNG 관계
;각인 변동이 방아쇠인 대사
;각인 변동 후에 대사를 발동합니다.
;==================================================
@KOJO_MESSAGE_MARKCNG_248
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;재갈이 있을 경우에도 돌아온다
SIF TEQUIP:42
    RETURN 0
;기절 중에도 돌아온다
SIF TFLAG:899 >= 1
    RETURN 0
;각인 취득 대사 호출
FOR LOCAL, 0, 7
    FOR LOCAL:1, 1, 4
        SIF TFLAG:(LOCAL + 40) == LOCAL:1 && CFLAG:(LOCAL + 5310) < LOCAL:1
            TRYCALLFORM K248_MESSAGE_MARK{LOCAL}_{LOCAL:1}
    NEXT
NEXT

;==================================================
;각인 취득시의 대사 CFLAG 5310～을 사용
;==================================================
;-------------------------------------------------
;고통각인 취득 CFLAG:5310
;-------------------------------------------------
@K248_MESSAGE_MARK0_1
PRINTFORMW 「아파,아파,아파,아파!,아파앗!!」
CFLAG:5310 = 1

@K248_MESSAGE_MARK0_2
PRINTFORMW 「아파……, 조금만 살살해……줘,」
CFLAG:5310 = 2

@K248_MESSAGE_MARK0_3
PRINTFORMW 「아아앗……!! 아파…………! 자, 잘못했어……!」
CFLAG:5310 = 3

;-------------------------------------------------
;쾌락각인 취득 CFLAG:5311
;-------------------------------------------------
@K248_MESSAGE_MARK1_1
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「언니……? 기분이 이상해……」
    PRINTFORMW 「이상하니까 그만 만져……!」
ELSE
    PRINTFORMW 「후…… 후아?! 기분이 이상해……!」
ENDIF
CFLAG:5311 = 1

@K248_MESSAGE_MARK1_2
PRINTFORMW 「이, 이런 기, 기분, 처……, 처음, 느, 느껴보는데?!」
CFLAG:5311 = 2

@K248_MESSAGE_MARK1_3
IF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「어, 언니, 하읏, 그, 그만, 꺄아앗!……」
ELSE
    PRINTFORMW 「으핫, 하읏, 후아, 아앙, 아아앗!, 하흣!!,」
ENDIF
CFLAG:5311 = 3

;-------------------------------------------------
;굴복각인 취득 CFLAG:5312
;-------------------------------------------------
@K248_MESSAGE_MARK2_1
PRINTFORMW 「그, 그런다고 내가 겁먹을 줄 알아!」
CFLAG:5312 = 1

@K248_MESSAGE_MARK2_2
PRINTFORMW 「충분 하잖아……,」
CFLAG:5312 = 2

@K248_MESSAGE_MARK2_3
PRINTFORMW 「그만……,」
CFLAG:5312 = 3

;-------------------------------------------------
;공포각인 취득 CFLAG:5313
;-------------------------------------------------
@K248_MESSAGE_MARK3_1
PRINTFORMW 「까아……!」
CFLAG:5313 = 1

@K248_MESSAGE_MARK3_2
PRINTFORMW 「그,그, 그만해……!」
CFLAG:5313 = 2

@K248_MESSAGE_MARK3_3
PRINTFORMW 「……잘못했어요…………!」
CFLAG:5313 = 3

;-------------------------------------------------
;반발각인 취득 CFLAG:5314
;-------------------------------------------------
@K248_MESSAGE_MARK4_1
PRINTFORMW 「이런 짓을 하고도, 멀쩡할 꺼라 생각해?」
CFLAG:5314 = 1

@K248_MESSAGE_MARK4_2
PRINTFORMW 「가만 안 둬, 가만 안 둬, 가만 안 둬, 가만 안 둬!!」
CFLAG:5314 = 2

@K248_MESSAGE_MARK4_3
PRINTFORMW 「저주할꺼야……, 평생의 운까지 전부 빨아들일테니까……!!」
CFLAG:5314 = 3

;-------------------------------------------------
;치욕각인 취득 CFLAG:5316
;-------------------------------------------------
@K248_MESSAGE_MARK6_1
PRINTFORMW 「큿, 그렇게 하면 즐거워……?」
CFLAG:5316 = 1

@K248_MESSAGE_MARK6_2
PRINTFORMW 「보……보지마……,」
CFLAG:5316 = 2

@K248_MESSAGE_MARK6_3
PRINTFORMW 「……흐아, 보지 마, 제발…………」
CFLAG:5316 = 3

;==================================================
;@KOJO_MESSAGE_COM 관계
;커맨드 대사 판정 처리
;커맨드 실행시에 출력됩니다
;==================================================
@KOJO_MESSAGE_COM_248
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;재갈이 있고, 재갈을 채우는 커맨드가 아닐 경우에도 돌아온다
SIF (TEQUIP:42 && (SELECTCOM != 226 && SELECTCOM != 229 && SELECTCOM != 506 && SELECTCOM != 509 && SELECTCOM != 583))
    RETURN 0
;기절 중에도 돌아온다
SIF TFLAG:899 >= 1
    RETURN 0

;커맨드 첫회 판정을 갱신
CALL COM_FLAG
;PANCTION 내의 장비품 대사 보완 함수를 사용한다
SIF FLAG:10 == 1
    TRYCALL EQUIPTEXT
;처녀 상실시의 메시지 호출
SIF TALENT:0 && TFLAG:1
    TRYCALL K248_MESSAGE_LOST_VIRGIN
;커맨드 대사 호출
TRYCALLFORM K248_MESSAGE_COM_{SELECTCOM}

;-------------------------------------------------
;처녀 상실
;-------------------------------------------------
@K248_MESSAGE_LOST_VIRGIN
;연모시에다 주인의 페니스
IF TFLAG:2
    PRINTFORMW 「아파……, 안아 줘……,」
;그 외
ELSEIF ASSIPLAY && ASSINO() == 249
    PRINTFORMW 「어……, 언니……?」
    PRINTFORMW 『조온, 미안해……,』
ELSE
    PRINTFORMW 「아파,아파,아파,아파,아파!!」
ENDIF

;==================================================
;@SELF_KOJO 관계
;이벤트 대사 판정 처리
;SELF_KOJO 실행시에 출력됩니다
;==================================================
@SELF_KOJO_K248
;자고 있을 경우에는 돌아온다
SIF EQUIP:14
    RETURN 0
;조교 중에다 재갈을 물렸을 때에도 돌아온다
SIF TFLAG:999 && TEQUIP:42
    RETURN 0
;조교 중에다 기절 중일 때에도 돌아온다
SIF TFLAG:999 && (TFLAG:899 >= 1 && TFLAG:200 != 12)
    RETURN 0

;이벤트 대사 호출
TRYCALLFORM K248_MESSAGE_EVENT_{(TFLAG:200)}

;==================================================
;@LOOK_VIDEO_KOJO 관계
;비디오를 함께 감상할 시의 대사
;==================================================
@LOOK_VIDEO_KOJO_K248
;PRINTFORMW 「비디오를 함께 감상할 시의 대사입니다」

;==================================================
;@DAILY_LIFE_MASSAGE 관계
;휴식 대사
;일상생활 이벤트ＯＮ이며 휴식 실행시에 출력됩니다
;==================================================
@DAILY_MESSAGE_248, ARG
IF MARK:9 >= 3
    PRINTFORMW 「휴식? 잔뜩 써주겠어!」
ELSEIF CFLAG:1610 == 0
    PRINTFORMW 「일 없으면 그냥 보내주면 좋겠는데……,」
ELSE
    PRINTFORMW 「오늘 쉬는거야?」
ENDIF

;==================================================
;@IMPRESSION_MAIN 관계
;스테이터스 화면을 볼 시에 출력됩니다
;한 마디 표시이므로 한 줄로 정리할 것
;==================================================
@IMPRESSION_K248
;아직 한 번도 조교하지 않았음
IF CFLAG:1610 == 0
    IF RAND:3 == 0
        PRINTSINGLEFORM    「하암……,」
    ELSEIF RAND:2 == 0
        PRINTSINGLEFORM    「돈이 다 떨어져가……,」
    ELSE
        PRINTSINGLEFORM    「여긴 어디야? 졸립고 피곤한데……,」
    ENDIF
    RETURN 1
;반발각인 3
ELSEIF MARK:9 >= 3
    IF MARK:2 >= 3
        IF RAND:3 == 0
            PRINTSINGLEFORM    「가만 안둘꺼야……, 가만 안둘꺼야……,」
        ELSEIF RAND:2 == 0
            PRINTSINGLEFORM    「저주할꺼야……,」
        ELSE
            PRINTSINGLEFORM    「복수하고 싶어……, 하지만 못된짓을 할까봐 무서워……,」
        ENDIF
    ELSE
        IF RAND:3 == 0
            PRINTSINGLEFORM    「역병신을 건드리다니! 후회하게 해주지!」
        ELSEIF RAND:2 == 0
            PRINTSINGLEFORM    「저주할꺼야! 평생 달라붙어서 쓸개까지 빨아먹을꺼야!」
        ELSE
            PRINTSINGLEFORM    「가만 안둘꺼야! 빈털털이로 만들어서 평생 거지꼴로 만들어 주겠어!」
        ENDIF
    ENDIF
    RETURN 1
;상애
ELSEIF TALENT:153
    IF RAND:3 == 0
        PRINTSINGLEFORM    「돈을 주면 나만 바라봐 줄까……?」
    ELSEIF RAND:2 == 0
        PRINTSINGLEFORM    「돈보다 더 좋은거……, 찾은거 같아……,」
    ELSE
        PRINTSINGLEFORM    「돈 같은거 이제 필요없어, 정말 필요한게 옆에 있는데,」
    ENDIF
    RETURN 1
;연모
ELSEIF TALENT:150
    IF RAND:3 == 0
        PRINTSINGLEFORM    「좋아해……,」
    ELSEIF RAND:2 == 0
        PRINTSINGLEFORM    「왜 난 역병신인거지……? 좋아하는 사람을 빈곤하게 만드는……,」
    ELSE
        PRINTSINGLEFORM    「돈 따위로는 사람의 마음을 잡을 수 없는걸까……?」
    ENDIF
    RETURN 1
;굴복각인 3
ELSEIF MARK:2 >= 3 && MARK:9 < 3
    IF RAND:3 == 0
        PRINTSINGLEFORM    「아……, 혼자있고 싶어……,」
    ELSEIF RAND:2 == 0
        PRINTSINGLEFORM    「옷이 너덜너덜 거려……, 새거 사야하는데……,」
    ELSE
        PRINTSINGLEFORM    「돈 안돌려줘도 되니까, 이제 그만하고 싶어……,」
    ENDIF
    RETURN 1
;그 외
ELSE
    IF RAND:3 == 0
        PRINTSINGLEFORM    「여기 있기 싫어……,」
    ELSEIF RAND:2 == 0
        PRINTSINGLEFORM    「돈이 다 떨어져가……,」
    ELSE
        PRINTSINGLEFORM    「대체 누구지? 왜 이런짓을 하는거야……,」
    ENDIF
    RETURN 1
ENDIF

;=============================================================================
;엔딩 대사의 처리
;=============================================================================
;-------------------------------------------------
;@SINGLE_ENDING 관계
;단독 엔딩
;-------------------------------------------------
@SINGLE_ENDING_K248
DRAWLINE
PRINTFORMW ―――단독 엔딩 조건 달성―――
PRINTFORMW …………
PRINTFORMW ………
PRINTFORMW ……
;PRINTFORMW 「단독 엔딩 달성시의 대사입니다」
PRINTFORMW  
PRINTFORMW 　　　　　　　　―― Ending No.{1000+NO:TARGET} (%CALLNAME:TARGET% 엔딩)
RETURN 0
;-------------------------------------------------
;@SINGLE_ENDING_AFTER 관계
;단독 엔딩 후에 「계속한다」를 선택했을 때에 출력합니다
;-------------------------------------------------
@SINGLE_ENDING_AFTER_K248
;PRINTFORMW 「단독 엔딩 종료 후의 대사입니다」
RETURN 0

;-------------------------------------------------
;@SLAVE_ENDING 관계
;노예 엔딩
;-------------------------------------------------
@SLAVE_ENDING_K248
DRAWLINE
PRINTFORMW ―――노예 엔딩 조건 달성―――
PRINTFORMW …………
PRINTFORMW ………
PRINTFORMW ……
;PRINTFORMW 「노예 엔딩 달성시의 대사입니다」
PRINTFORMW  
PRINTFORMW 　　　　　　　　―― Ending No.{1000+NO:TARGET}i (%CALLNAME:TARGET% 노예 엔딩)
RETURN 0
;-------------------------------------------------
;@SLAVE_ENDING_AFTER 관계
;노예 엔딩 후에 「계속한다」를 선택했을 때에 출력합니다
;-------------------------------------------------
@SLAVE_ENDING_AFTER_K248
;PRINTFORMW 「노예 엔딩 종료 후의 대사입니다」
RETURN 0

;=============================================================================
;매각 후 에피소드
;엔딩 시에 판 캐릭터의 에피소드를 삽입할 수 있다
;=============================================================================
;-------------------------------------------------
;에피소드 번호 결정 등
;-------------------------------------------------
;@SET_SLAVE_AFTERWARDS_K248
;대상을 퇴피해서 TFLAG:710를 대상으로 삼는다.
;LOCAL = TARGET
;TARGET = TFLAG:710

;음란
;IF TALENT:170
;    LOCAL:1 = 1
;에피소드 없음
;ELSE
;    LOCAL:1 = 0
;ENDIF

;퇴피한 대상을 원래대로 되돌린다
;TARGET = LOCAL
;RETURN LOCAL:1

;-------------------------------------------------
;에피소드 표시
;-------------------------------------------------
;@SHOW_SLAVE_AFTERWARDS_KX1, ARG, ARG:1
;에피소드 표시
;DRAWLINE
;PRINTFORMW 　%CSVCALLNAME(X1,0)%의 그 이후
;DRAWLINE
;IF ARG == 1
    ;PRINTFORMW 음란 에피소드
;ENDIF
;DRAWLINE
;RETURN 0

;=============================================================================
;eratohoYM용 프롤로그(및 그에 대응한 에필로그)의 예시
;프롤로그 No.X1 -(부제 입력란)-
;=============================================================================
;--------------------------------------------------
;프롤로그의 존재 판정
;--------------------------------------------------
;「RETURN 0」을 「RETURN 1」로 고치면 읽기 가능해진다
@EXIST_PROLOGUE_YM_248
RETURN 0

;--------------------------------------------------
;프롤로그의 표시
;--------------------------------------------------
;선택 화면에서 제목을 기입할 경우에는 이곳에서
@SHOW_PROLOGUE_YM_248
PRINTL [X1] 프롤로그 No.248 -(부제 입력란)-

;--------------------------------------------------
;프롤로그 No.X1 -(부제 입력란)-본문
;--------------------------------------------------
@EXE_PROLOGUE_YM_248
PRINTW 　　　　　　　　―― Prologue No.X1 -(부제 입력란)- (eratohoYM)
PRINTW  
DRAWLINE
PRINTL -------------------------------------GOODLUCK------------------------------------
DRAWLINE
PRINTW 

;=============================================================================
;프롤로그 No.X1에 대응한 에필로그
;=============================================================================
;표시시킨 프롤로그에 대응하는 에필로그를 노멀 엔딩 대신에 표시시키는 것이 가능
;더해서, 시스템 상 프롤로그를 작성하지 않으면 표시되지 않는다
;--------------------------------------------------
;프롤로그 No.X1에 대응한 에필로그의 존재 판정
;--------------------------------------------------
;「RETURN 0」을 「RETURN 1」로 고치면 읽기 가능해진다
@EXIST_EPILOGUE_YM_248
RETURN 0

;--------------------------------------------------
;프롤로그 No.X1에 대응한 에필로그 본문
;--------------------------------------------------
@EXE_EPILOGUE_YM_248
PRINTW  
PRINTW 　　　　　　　　―― Ending No.01 (노멀 엔딩)

;■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
;■　　　　　　　　　　　　　 이하 EXTRA 모드용　　　　　　　　　　　　　　　　　　■
;■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■■
;EXTRA 모드에서는 주인이 된 캐릭터의 대사를 읽어들입니다
;eratohoYM용와는 다르게 표시 가능한 건 디폴트거나 주인 대사 중 한쪽뿐이므로, 선택지 표시용의 함수는 존재하지 않습니다
;
;예)○○는 예전부터 사모하고 있던 ××을 수중에 넣고자, 행동을 개시했다…… 같은 느낌?
;
;추가로 프롤로그에서 캐릭터를 추가하고 싶을 경우, 캐릭터 ID 관계상 직접 「ADDCHARA Ｘ」를 쓰는 것보다
;「CALL CHARA_STATE_BASIC, Ｘ」를 쓰는 편이 바람직하다.
;그때, 자동적으로 가입한 캐릭터가 조교 대상으로서 설정된다.
;또, 「CALL CHARA_STATE_BASIC, Ｘ」 뒤에 「CSTR:3 = ～～～～」를 입력하면,
;어떠한 방법으로 끌려왔는지 기억하게 할 수 있다.
;=============================================================================
;프롤로그 No.X1 -(부제 입력란)-
;=============================================================================
;--------------------------------------------------
;프롤로그 No.X1의 존재 판정
;--------------------------------------------------
;「RETURN 0」을 「RETURN 1」로 고치면 읽기 가능(존재하고 있다고 판단)해진다
@EXIST_PROLOGUE_EXTRA_248
RETURN 0

;--------------------------------------------------
;프롤로그 No.X1 -(부제 입력란)-본문
;--------------------------------------------------
@EXE_PROLOGUE_EXTRA_248

;………………………………………………
;캐릭터 추가 이벤트를 삽입할 경우
;………………………………………………
;제작자 각자의 판단으로 프롤로그에서 캐릭터를 추가하는 것이 가능합니다.
;단, 초기 금액 이상의 가격인 캐릭터를 추가할 경우,
;최악으론 게임 밸런스가 붕괴할 가능성이 있습니다. 그 점을 감안하여 이벤트를 추가해 주십시오.
;
;특정 캐릭터를 추가할 경우(Ｘ에는 캐릭터 번호를 입력)
;CALL CHARA_STATE_BASIC, Ｘ
;어떻게 끌려왔는지를 기억(예: CSTR:3 = 유괴했다.)
;※지나치게 길면 표시가 삐뚤어질 가능성 있음
;CSTR:3 = 
;이하의 행을 해금하면 위에서 추가한 캐릭터를 구입한 걸로 할 수 있음(＝소지금이 준다. 단, 0 미만은 되지 않음)
;MONEY = MIN(MONEY - ITEMPRICE:(NO:TARGET), 0)
;………………………………………………

PRINTW 　　　　　　　　―― EXTRA Prologue No.248 -(부제 입력란)-
PRINTW  
DRAWLINE
PRINTL ------------------------------------LET’S ENJOY----------------------------------
DRAWLINE
PRINTW 
